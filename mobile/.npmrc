# @config-plugins/react-native-blob-util@13 declares a peer dep on expo@^55, but this
# project is pinned to expo ~54 — legacy-peer-deps lets npm install proceed anyway
# (this has been the working combination all along; EAS Build's install step needs it too).
legacy-peer-deps=true
